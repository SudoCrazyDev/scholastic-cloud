name = "document-reader"
main = "src/index.js"
compatibility_date = "2024-12-01"

# Workers AI binding for image text extraction (Llama 3.2 Vision)
[ai]
binding = "AI"

# Optional: increase limits for larger PDFs/images
[limits]
cpu_ms = 30000
